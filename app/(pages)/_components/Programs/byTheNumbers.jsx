{/* write a function call here that deos the by the numbers*/ }
//import styles.
import styles from "./byTheNumbers.module.scss";
export default function byNumbers({ value, description, image }) {
  return (
    
  );
}